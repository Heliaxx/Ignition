[gd_scene load_steps=16 format=3 uid="uid://d3mqn4lo1yhif"]

[ext_resource type="Script" uid="uid://n875xpqjkl8q" path="res://Scripts/LevelFreeFlight.cs" id="1_xijtb"]
[ext_resource type="Texture2D" uid="uid://bsvhlcw4w5oev" path="res://Imports/Skyboxes/2k_stars_milky_way.jpg" id="3_02vup"]
[ext_resource type="PackedScene" path="res://Scenes/Station4.tscn" id="4_d3pwp"]
[ext_resource type="PackedScene" uid="uid://bkmrv7xhowrhy" path="res://Scenes/kaito.tscn" id="5_3qx6a"]
[ext_resource type="PackedScene" uid="uid://p8bmcn3ds03m" path="res://Scenes/FpsMeter.tscn" id="7_6pirs"]
[ext_resource type="PackedScene" uid="uid://dmy55noj8bktd" path="res://Scenes/PauseMenu.tscn" id="8_63mkt"]
[ext_resource type="Script" uid="uid://bwpdy447817x3" path="res://Scripts/ChunkedAsteroidField.cs" id="9_6rtd5"]
[ext_resource type="Texture2D" uid="uid://eqbd7lweaw6q" path="res://Imports/Planets/earth/textures/Material_62_baseColor.png" id="10_wh56p"]
[ext_resource type="Texture2D" uid="uid://byh22h6q3rc0d" path="res://Imports/Planets/earth/textures/Material_62_emissive.jpeg" id="11_n0ucd"]
[ext_resource type="Texture2D" uid="uid://b1318syjhu2cq" path="res://Imports/Planets/Rings/2k_saturn_ring_alpha.png" id="12_ringt"]

[sub_resource type="Shader" id="Shader_6tkp5"]
code = "shader_type sky;

uniform sampler2D sky_tex: source_color, filter_linear, hint_default_black;
uniform sampler2D planet_albedo_tex: source_color, filter_linear, hint_default_black;
uniform sampler2D planet_night_tex: source_color, filter_linear, hint_default_black;
uniform vec3 sun_pos =  vec3(0.0);
uniform bool planet_enabled = true;
uniform vec3 planet_direction = vec3(-0.55, 0.12, -0.82);
uniform float planet_distance = 24.0;
uniform float planet_radius = 6.2;
uniform float planet_rotation = 0.0;
uniform float planet_night_strength: hint_range(0.0, 4.0, 0.01) = 0.75;
uniform vec3 planet_tint: source_color = vec3(1.0);
uniform vec3 atmosphere_color: source_color = vec3(0.24, 0.48, 1.0);
uniform float atmosphere_strength: hint_range(0.0, 4.0, 0.01) = 0.9;
uniform float atmosphere_falloff: hint_range(0.5, 16.0, 0.01) = 5.2;
uniform bool sun_enabled = true;
uniform vec3 sun_color: source_color = vec3(1.0, 0.62, 0.28);
uniform vec3 sun_core_color: source_color = vec3(1.0, 0.95, 0.86);
uniform float sun_disc_size: hint_range(0.0005, 0.08, 0.0001) = 0.008;
uniform float sun_disc_softness: hint_range(0.05, 2.0, 0.01) = 0.45;
uniform float sun_halo_strength: hint_range(0.0, 8.0, 0.01) = 1.4;
uniform float sun_halo_falloff: hint_range(2.0, 256.0, 0.1) = 18.0;
uniform float sun_glare_strength: hint_range(0.0, 4.0, 0.01) = 0.22;

// Ring system
uniform bool ring_enabled = false;
uniform float ring_inner_radius: hint_range(0.1, 20.0, 0.1) = 7.0;
uniform float ring_outer_radius: hint_range(0.1, 30.0, 0.1) = 12.0;
uniform vec3 ring_color: source_color = vec3(0.7, 0.65, 0.55);
uniform float ring_opacity: hint_range(0.0, 1.0, 0.01) = 0.6;
uniform vec3 ring_normal = vec3(0.0, 1.0, 0.0); // Ring plane normal (up = horizontal ring)
uniform sampler2D ring_texture: filter_linear, hint_default_white;

struct RayHit {
	bool is_hit;
	vec3 normal;
	vec3 hit_point;
};

RayHit hit_sphere(vec3 ray_origin, vec3 ray_dir, vec3 sphere_center, float sphere_radius) {
	RayHit ray_hit;
	ray_hit.is_hit = false;
	vec3 oc = ray_origin - sphere_center;
	float a =  dot(ray_dir, ray_dir);
	float b = 2.0 * dot(oc, ray_dir);
	float c = dot (oc, oc) - sphere_radius * sphere_radius;
	float delta = b * b - 4.0 * a * c;
	if (delta < 0.0) {
		return ray_hit;
	}
	float root = sqrt(delta);
	float t1 = (-b - root) / (2.0 * a);
	float t2 = (-b + root) / (2.0 * a);
	float t_near = min(t1, t2);
	float t_far = max(t1, t2);
	if (t_near < 0.0) {
		if (t_far >= 0.0) {
			t_near = 0.0;
		} else {
			return ray_hit;
		}
	}
	ray_hit.hit_point = fma(ray_dir, vec3(t_near), ray_origin);
	ray_hit.normal = normalize(ray_hit.hit_point - sphere_center);
	ray_hit.is_hit = true;
	return ray_hit;
}

// Ring intersection: returns hit info for a flat disc/ring around a center point
struct RingHit {
	bool is_hit;
	vec3 hit_point;
	float ring_u; // 0-1 from inner to outer radius
};

RingHit hit_ring(vec3 ray_origin, vec3 ray_dir, vec3 ring_center, vec3 ring_normal_dir, float inner_r, float outer_r) {
	RingHit ring_hit;
	ring_hit.is_hit = false;

	// Ray-plane intersection
	float denom = dot(ring_normal_dir, ray_dir);
	if (abs(denom) < 0.0001) {
		return ring_hit; // Ray parallel to ring plane
	}

	float t = dot(ring_center - ray_origin, ring_normal_dir) / denom;
	if (t < 0.0) {
		return ring_hit; // Ring behind camera
	}

	vec3 hit_point = ray_origin + ray_dir * t;
	float dist = length(hit_point - ring_center);

	if (dist >= inner_r && dist <= outer_r) {
		ring_hit.is_hit = true;
		ring_hit.hit_point = hit_point;
		ring_hit.ring_u = (dist - inner_r) / (outer_r - inner_r);
	}
	return ring_hit;
}

vec2 get_spherical_uv(vec3 point_on_sphere, float rotation) {
vec3 n = normalize(point_on_sphere);
float theta = atan(n.z, n.x);
float phi = acos(n.y);
theta += rotation;
float u = (theta + PI) / TAU;
float v = phi / PI;
return vec2(u, v);
}

vec3 compute_Lighting(vec3 normal, vec3 light_dir) {
	return vec3(max(dot(normal, -light_dir), 0.0));
}

void sky() {
	if(AT_CUBEMAP_PASS) {
		COLOR = texture(sky_tex, SKY_COORDS).rgb;
	} else {
		COLOR = texture(sky_tex, SKY_COORDS).rgb;
		vec3 light_dir = normalize(sun_pos);
		bool hit_planet = false;
		float planet_t = 1e10; // Distance to planet hit
		vec3 planet_color_result = vec3(0.0);

		if (planet_enabled) {
			vec3 planet_dir = normalize(planet_direction);
			vec3 planet_center = planet_dir * planet_distance;
			RayHit planet_hit = hit_sphere(vec3(0.0), normalize(EYEDIR), planet_center, planet_radius);
			if (planet_hit.is_hit) {
				hit_planet = true;
				planet_t = length(planet_hit.hit_point);
				vec2 planet_uv = get_spherical_uv(planet_hit.normal, planet_rotation);
				vec3 albedo = texture(planet_albedo_tex, planet_uv).rgb * planet_tint;
				float ndotl = max(dot(planet_hit.normal, -light_dir), 0.0);
				vec3 day_color = albedo * (0.07 + ndotl * 1.25);
				vec3 night_mask = texture(planet_night_tex, planet_uv).rgb;
				vec3 night_color = night_mask * (1.0 - ndotl) * planet_night_strength;
				float rim = pow(1.0 - max(dot(planet_hit.normal, -EYEDIR), 0.0), atmosphere_falloff);
				vec3 atmosphere = atmosphere_color * rim * atmosphere_strength;
				planet_color_result = day_color + night_color + atmosphere;
			}
		}

		// Ring rendering
		bool hit_ring_result = false;
		float ring_t = 1e10;
		vec3 ring_color_result = vec3(0.0);
		float ring_alpha = 0.0;

		if (ring_enabled && planet_enabled) {
			vec3 planet_dir = normalize(planet_direction);
			vec3 planet_center = planet_dir * planet_distance;
			vec3 norm_ring_normal = normalize(ring_normal);

			RingHit r_hit = hit_ring(vec3(0.0), normalize(EYEDIR), planet_center, norm_ring_normal, ring_inner_radius, ring_outer_radius);
			if (r_hit.is_hit) {
				hit_ring_result = true;
				ring_t = length(r_hit.hit_point);

				// Sample ring texture (u = radial position 0-1)
				vec2 ring_uv = vec2(r_hit.ring_u, 0.5);
				vec4 ring_tex = texture(ring_texture, ring_uv);

				// Apply simple lighting
				float ring_ndotl = abs(dot(norm_ring_normal, -light_dir)) * 0.6 + 0.4;
				ring_color_result = ring_color * ring_tex.rgb * ring_ndotl;
				ring_alpha = ring_opacity * ring_tex.a;

				// Check if ring is in planet's shadow (simple check)
				// If the ring point is on the opposite side of the planet from the sun, darken it
				vec3 ring_to_planet = planet_center - r_hit.hit_point;
				float shadow_check = dot(normalize(ring_to_planet), -light_dir);
				if (shadow_check > 0.0 && length(ring_to_planet) < planet_radius * 1.1) {
					ring_color_result *= 0.3; // In shadow
				}
			}
		}

		// Composite: planet and ring with depth ordering
		if (hit_planet && hit_ring_result) {
			if (ring_t < planet_t) {
				// Ring in front of planet
				COLOR = mix(planet_color_result, ring_color_result, ring_alpha);
			} else {
				// Ring behind planet - planet fully occludes
				COLOR = planet_color_result;
			}
		} else if (hit_planet) {
			COLOR = planet_color_result;
		} else if (hit_ring_result) {
			// Ring visible, blend with sky
			COLOR = mix(COLOR, ring_color_result, ring_alpha);
		}

		if (sun_enabled && !hit_planet) {
			vec3 view_dir = normalize(EYEDIR);
			float to_sun = max(dot(view_dir, -light_dir), 0.0);
			float disc_edge = 1.0 - sun_disc_size;
			float disc = smoothstep(disc_edge, min(1.0, disc_edge + sun_disc_size * max(sun_disc_softness, 0.001)), to_sun);
			float core = pow(disc, 2.2);
			float halo = pow(to_sun, sun_halo_falloff) * sun_halo_strength;
			float glare = pow(to_sun, max(2.0, sun_halo_falloff * 0.35)) * sun_glare_strength;
			vec3 sun_render = halo * sun_color + core * sun_core_color + glare * sun_color;
			COLOR += sun_render;
		}
	}
}"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_7645s"]
shader = SubResource("Shader_6tkp5")
shader_parameter/sky_tex = ExtResource("3_02vup")
shader_parameter/planet_albedo_tex = ExtResource("10_wh56p")
shader_parameter/planet_night_tex = ExtResource("11_n0ucd")
shader_parameter/sun_pos = Vector3(2000, 0, 1000)
shader_parameter/planet_enabled = true
shader_parameter/planet_direction = Vector3(-6.66, -2, -24)
shader_parameter/planet_distance = 14.0
shader_parameter/planet_radius = 6.0
shader_parameter/planet_rotation = 0.15
shader_parameter/planet_night_strength = 0.75
shader_parameter/planet_tint = Color(1, 1, 1, 1)
shader_parameter/atmosphere_color = Color(0.561713, 0.710745, 1, 1)
shader_parameter/atmosphere_strength = 0.7
shader_parameter/atmosphere_falloff = 5.0
shader_parameter/sun_enabled = true
shader_parameter/sun_color = Color(1, 0.649088, 0.349364, 1)
shader_parameter/sun_core_color = Color(1, 0.933333, 0.933333, 1)
shader_parameter/sun_disc_size = 0.002
shader_parameter/sun_disc_softness = 0.5
shader_parameter/sun_halo_strength = 1.4
shader_parameter/sun_halo_falloff = 22.0
shader_parameter/sun_glare_strength = 0.22
shader_parameter/ring_enabled = true
shader_parameter/ring_inner_radius = 6.9
shader_parameter/ring_outer_radius = 20.1
shader_parameter/ring_color = Color(0.785271, 0.7416, 0.654445, 1)
shader_parameter/ring_opacity = 0.8
shader_parameter/ring_normal = Vector3(-0.1, 1, 0.015)
shader_parameter/ring_texture = ExtResource("12_ringt")

[sub_resource type="Sky" id="Sky_w7017"]
sky_material = SubResource("ShaderMaterial_7645s")

[sub_resource type="Environment" id="Environment_r05y7"]
background_mode = 2
sky = SubResource("Sky_w7017")
sky_rotation = Vector3(0, 3.14159, 0)
ambient_light_source = 2
ambient_light_color = Color(1, 1, 1, 1)
ambient_light_energy = 0.15
reflected_light_source = 1
tonemap_mode = 2
ssao_intensity = 16.0
ssao_detail = 5.0
ssao_sharpness = 0.0
glow_levels/1 = 7.1
glow_levels/2 = 7.35
glow_levels/3 = 6.45
glow_levels/4 = 6.69
glow_levels/5 = 2.85
glow_levels/7 = 6.85
glow_normalized = true
glow_mix = 0.113
glow_hdr_luminance_cap = 41.19
fog_mode = 1
fog_light_color = Color(0.337255, 0.584314, 0.72549, 1)
fog_light_energy = 3.2
fog_sun_scatter = 0.45
fog_density = 0.0
fog_aerial_perspective = 0.19
fog_sky_affect = 0.0
fog_height = 300.0
fog_height_density = 0.002
volumetric_fog_enabled = true
volumetric_fog_density = 0.0002
volumetric_fog_albedo = Color(1, 0.866047, 0.715673, 1)
volumetric_fog_emission_energy = 0.0
volumetric_fog_gi_inject = 0.5
volumetric_fog_anisotropy = 0.5
volumetric_fog_length = 1000.0
volumetric_fog_sky_affect = 0.412
volumetric_fog_temporal_reprojection_enabled = false
volumetric_fog_temporal_reprojection_amount = 0.5

[sub_resource type="NavigationMesh" id="NavigationMesh_qi7iq"]

[node name="Level" type="Node3D"]
script = ExtResource("1_xijtb")

[node name="WorldEnvironment" type="WorldEnvironment" parent="."]
environment = SubResource("Environment_r05y7")

[node name="NavigationRegion3D" type="NavigationRegion3D" parent="."]
navigation_mesh = SubResource("NavigationMesh_qi7iq")

[node name="Station" parent="NavigationRegion3D" instance=ExtResource("4_d3pwp")]

[node name="Player" parent="NavigationRegion3D" instance=ExtResource("5_3qx6a")]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, -500, 0, 0)
UnlimitedAmmo = true

[node name="DirectionalLight3D" type="DirectionalLight3D" parent="."]
transform = Transform3D(-0.573577, 0, -0.819152, 0, 1, 0, 0.819152, 0, -0.573577, 0, 0, -20)
light_bake_mode = 0
sky_mode = 1

[node name="FPSMeter" parent="." instance=ExtResource("7_6pirs")]
mouse_filter = 2

[node name="Scenery" type="Node3D" parent="."]

[node name="PauseMenuLayer" type="CanvasLayer" parent="."]
process_mode = 3
layer = 3

[node name="PauseMenu" parent="PauseMenuLayer" instance=ExtResource("8_63mkt")]

[node name="ChunkedAsteroidField" type="Node3D" parent="." node_paths=PackedStringArray("SpawnExclusionZones")]
script = ExtResource("9_6rtd5")
LoadRadius = 15
CollisionRadius = 5
LimitUpward = true
MinSpacing = 500.0
MinScale = 30.0
MaxScale = 90.0
SpawnExclusionZones = [NodePath("../NavigationRegion3D/Player"), NodePath("../NavigationRegion3D/Station")]
